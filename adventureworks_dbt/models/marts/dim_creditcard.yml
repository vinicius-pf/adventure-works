version: 2

models:
  - name: dim_creditcard
    description: "Contains information about the credit cards."
    columns:
      - name: credit_card_id
        description: 'Primary key of the table.'
        tests: 
          - unique
          - not_null
      
      - name: card_type
        description: 'Type of credit card'